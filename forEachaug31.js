/*function clean(item) {
    console.log(`I just cleaned a ${item}`)
  }
  
  const nails = ["rusty nail", "rusty nail", "bent nail", "clean nail"]
  
  for (let i = 0; i < nails.length; i++) {
    clean(nails[i])
  }
  
  const planks = ["splintered plank", "straight plank", "bent plank"]
  
  for (let i = 0; i < planks.length; i++) {
    clean(planks[i])
  }
  
  console.log('done!')



  // =========== ForEach Challenge

const recreateForEach = (array, callback) => {
  // loop over the array
  // call the callback on each of the element inside the array
}

let alphabet = '';
const letters = ['a', 'b', 'c', 'd'];

recreateForEach(letters, function(char) {
alphabet += char;
});
console.log(alphabet); //prints 'abcd'*/



/*function recreateForEach(array, cbFn) {
  console.log(`This is my favorite ${}`);

}

const myFav = ['music', 'coding', 'sports'] 
for(let i = 0; i < myFav.length; i++) {

}

console.log(myFav);*/



// =========== ForEach Challenge

const recreateForEach = (array, callback) => {
  // loop over the array
  // call the callback on each of the element inside the array
}

let alphabet = '';
const letters = ['a', 'b', 'c', 'd'];

recreateForEach(letters, function(char) {
alphabet += char;
});
console.log(alphabet); //prints 'abcd'